/**
 * Email verification is optional — users can shop immediately after signup.
 * A soft reminder is shown on the account profile until they verify or dismiss.
 * Set true only when you want to block login until the user clicks the email link.
 */
export const REQUIRE_EMAIL_VERIFICATION = false;
