/** Curtain pricing — feature module entry (implementation in @/lib/pricing). */
export {
  VAT_RATE,
  calcPrice,
  curtainTypeLabels,
  defaultConfigFor,
  fmtNum,
  fmtSoldK,
  fmtTHB,
  trackTypeLabels,
} from "@/lib/pricing";

export type { CurtainConfig, PriceBreakdown } from "@/lib/types";
